/**
 * 职责：存放通用、无业务含义的基础能力。
 * 规则：
 * 允许存放：
 *      统一返回结果、分页对象、通用异常、错误码枚举、通用常量、通用枚举、基础工具类、基础注解、基础实体
 * 禁止存放：
 *      业务逻辑、Controller、Service、Mapper、Entity、Redis 封装、短信封装、文件存储封装、第三方调用
 */
package com.imhgl.workbench.common;
